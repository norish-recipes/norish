import { BookOpenIcon as CookbookIconSolid } from "@heroicons/react/16/solid";
import { BookOpenIcon as CookbookIconOutline } from "@heroicons/react/24/outline";

/**
 * The one mark that means "cookbook".
 *
 * Named here rather than reached for per surface, so the empty cover, the
 * Library card and the recipe page's card cannot drift into three different
 * pictures of the same idea.
 */
export { CookbookIconOutline, CookbookIconSolid };
