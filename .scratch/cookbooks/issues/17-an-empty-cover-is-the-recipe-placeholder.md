# 17 — An empty cookbook uses the recipe placeholder

**What to build:** A cookbook with no member images rendered a tinted accent tile, which read as a different kind of object rather than as the same "nothing here yet". Supersedes "renders a plain tinted tile" in `03`.

**Status:** ready-for-human

- [x] Same surface, same muted mark, same opacity as a recipe with no picture
- [x] The cookbook mark in place of the photo one, so the placeholder still says which kind of row it is
